Use these local files as the planning source of truth:
- `.codexctl/README.md`
- `.codexctl/shapeup/README.md`
- `.codexctl/shapeup/shapeup-betting-table.md`
- `.codexctl/shapeup/shapeup-cycle-01.md`
- `.codexctl/shapeup/contracts/shapeup-contract-bet-01.md`

Rules already established in this repo:
- planning/process artifacts use `shapeup-*`
- `bet` is the executable unit
- `run` is the runtime record
- planning files stay under `.codexctl/`
- target user is a high-agency individual using AI agents to build products
- the product stance is strict Shape Up + DDD, not a generic automation DSL
